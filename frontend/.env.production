# SSO Hub authority for production builds. The backend runs as an IIS sub-application
# under /api on the same origin as this frontend, so tokens are same-origin (no CORS).
VITE_SSO_AUTHORITY=https://my.gcs-gresik.com/api
