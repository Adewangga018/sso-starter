<?xml version="1.0" encoding="utf-8"?>
<!--
  HANYA dipakai di build debug (android/app/src/debug/) - TIDAK PERNAH ikut ke build
  release/production (lihat mobile/README.md bagian dev). Mengizinkan cleartext (HTTP
  tanpa TLS) khusus ke backend dev lokal, karena Android 9+ (API 28+) memblokir
  cleartext secara default di level OS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
